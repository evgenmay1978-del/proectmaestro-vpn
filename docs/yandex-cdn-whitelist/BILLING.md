# Billing

Bytes are integers, units explicitly GB/GiB, money fixed precision/minor units. Tariff order: individual → plan → transport profile → global. Shadow mode only until explicit real-charging approval. Hard limit suspends only white-list entitlement.
