# Billing reconciliation

Compare per-user, instance and CDN aggregate totals with service/probe traffic. Reconciliation reports differences; it never mutates old ledger entries. Corrections are new reversal/adjustment entries.
