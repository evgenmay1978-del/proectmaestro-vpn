# Verified facts

Only sourced local/read-only evidence belongs here. Production deployment, live inventory, backup/restore, CDN switch, client matrix and billing are pending. No secret, endpoint, UUID, URI or customer data is recorded.
