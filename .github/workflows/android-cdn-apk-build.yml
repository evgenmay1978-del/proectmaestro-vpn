name: Build signed MaestroVPN APK only
on:
  workflow_call:
    inputs:
      capture_only:
        type: boolean
        default: false
      emulator_review:
        type: boolean
        default: false
      emulator_only:
        type: boolean
        default: false
      visual_preview:
        type: boolean
        default: false
  workflow_dispatch:
permissions:
  contents: read
  actions: read
jobs:
  apk:
    if: ${{ inputs.capture_only != true }}
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
          fetch-depth: 0
      - name: Require unchanged production sources for instrumentation-only
        if: ${{ inputs.emulator_only == true && inputs.visual_preview != true }}
        run: |
          set -euo pipefail
          [ "${{ inputs.emulator_review }}" = true ]
          git diff --exit-code e0b737d864e86062e1e5c3fe2fdfb26e663130f8 HEAD -- app/src/main app/build.gradle.kts
      - name: Preserve ordinary VPN engine
        run: git diff --exit-code 458fec7c885250f65d474204061dcdd60d71ee56 HEAD -- libbox-awg version.properties .github/workflows/libbox.yml
      - uses: actions/setup-java@cf277c60eb25467037889841efdb72551f06f6c3
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@9fc6c4e9069bf8d3d10b2204b1fb8f6ef7065407
      - uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('**/*.gradle.kts', 'settings.gradle.kts', 'version.properties') }}
          restore-keys: gradle-${{ runner.os }}-
      - name: Install pinned packaging toolchain
        run: |
          set -euo pipefail
          sdkmanager 'ndk;28.0.13004108' 'platforms;android-36' 'build-tools;36.0.0' >/dev/null
          echo "ANDROID_NDK_HOME=${ANDROID_SDK_ROOT}/ndk/28.0.13004108" >> "$GITHUB_ENV"
          mkdir -p "$RUNNER_TEMP/cdn-apk-inputs"
      - name: Reuse unchanged native libraries from signed 159
        if: ${{ inputs.emulator_only != true }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git diff --exit-code 6956ad75b0672b279217627542a30307a70410ef HEAD -- android-xhttp
          gh api "repos/$GITHUB_REPOSITORY/actions/artifacts/10118243638/zip" > "$RUNNER_TEMP/fleet-159.zip"
          python3 - <<'PY'
          import hashlib, os, pathlib, zipfile
          root = pathlib.Path(os.environ['RUNNER_TEMP'])
          with zipfile.ZipFile(root / 'fleet-159.zip') as z:
              names = [n for n in z.namelist() if n.endswith('.apk')]
              if len(names) != 1: raise SystemExit('baseline_apk_count')
              data = z.read(names[0])
          if hashlib.sha256(data).hexdigest() != '53270cee67b030509deaa8d12bf3c5076dff560afc709c65c5b90becfef0da65':
              raise SystemExit('baseline_apk_digest')
          baseline = root / 'fleet-159.apk'
          baseline.write_bytes(data)
          with zipfile.ZipFile(baseline) as z:
              for abi in ('arm64-v8a', 'armeabi-v7a'):
                  target = pathlib.Path('app/src/main/jniLibs') / abi
                  target.mkdir(parents=True, exist_ok=True)
                  for name in ('libmaestro_xhttp.so', 'libolcrtc.so'):
                      payload = z.read(f'lib/{abi}/{name}')
                      (target / name).write_bytes(payload)
                      print(abi, name, hashlib.sha256(payload).hexdigest())
          PY
      - name: Reuse exact libbox artifact
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh api "repos/$GITHUB_REPOSITORY/actions/artifacts/9203846250/zip" > "$RUNNER_TEMP/libbox.zip"
          python3 - <<'PY'
          import hashlib, os, pathlib, zipfile
          archive = pathlib.Path(os.environ['RUNNER_TEMP']) / 'libbox.zip'
          if hashlib.sha256(archive.read_bytes()).hexdigest() != 'baf1ae39b845601ef5291c92c9638261f4a9dfd7725d04ffd856431d48d42b37':
              raise SystemExit('libbox_archive_mismatch')
          with zipfile.ZipFile(archive) as z:
              members = [v for v in z.infolist() if pathlib.PurePosixPath(v.filename).name == 'libbox.aar']
              if len(members) != 1: raise SystemExit('libbox_member_mismatch')
              data = z.read(members[0])
          if hashlib.sha256(data).hexdigest() != 'c70ce917b331fd333d52b8e3c01eba2c2a343497f896fdf62c30436490a88f05':
              raise SystemExit('libbox_digest_mismatch')
          pathlib.Path('app/libs').mkdir(exist_ok=True)
          pathlib.Path('app/libs/libbox.aar').write_bytes(data)
          PY
      - name: Materialize stable signing keystore
        env:
          SIGNING_KEYSTORE_B64: ${{ secrets.SIGNING_KEYSTORE_B64 }}
          LOCAL_PROPERTIES: ${{ secrets.LOCAL_PROPERTIES }}
        run: |
          python3 - <<'PY'
          import base64, os, pathlib
          encoded = os.environ.get('SIGNING_KEYSTORE_B64', '')
          if not encoded or not os.environ.get('LOCAL_PROPERTIES'):
              raise SystemExit('stable_signing_secrets_required')
          raw = base64.b64decode(''.join(encoded.split()), validate=True)
          if not raw: raise SystemExit('stable_keystore_empty')
          with pathlib.Path('app/release.keystore').open('xb') as f:
              os.fchmod(f.fileno(), 0o600)
              f.write(raw)
          PY
      - name: Build signed APK 1.0.160 without test tasks
        if: ${{ inputs.emulator_only != true }}
        env:
          LOCAL_PROPERTIES: ${{ secrets.LOCAL_PROPERTIES }}
        run: |
          chmod +x ./gradlew
          ./gradlew :app:assembleOtherDebug -PmaestroCdnApk=true --stacktrace --no-daemon
      - name: Require installable identity and fleet signing compatibility
        id: apk
        if: ${{ inputs.emulator_only != true }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mapfile -t apks < <(find app/build/outputs/apk/other/debug -maxdepth 1 -type f -name '*.apk')
          [ "${#apks[@]}" -eq 1 ]
          apk="${apks[0]}"
          [ "$(apkanalyzer manifest application-id "$apk")" = 'com.maestrovpn.tv' ]
          [ "$(apkanalyzer manifest version-name "$apk")" = '1.0.160' ]
          [ "$(apkanalyzer manifest version-code "$apk")" = '1016002' ]
          [ "$(apkanalyzer manifest debuggable "$apk")" = 'false' ]
          signer="${ANDROID_SDK_ROOT}/build-tools/36.0.0/apksigner"
          "$signer" verify --verbose --print-certs "$apk" > "$RUNNER_TEMP/cdn-apk-inputs/signer.txt"
          "$signer" verify --print-certs "$RUNNER_TEMP/fleet-159.apk" > "$RUNNER_TEMP/fleet-159.signer"
          diff -u <(grep 'certificate SHA-256 digest:' "$RUNNER_TEMP/cdn-apk-inputs/signer.txt") <(grep 'certificate SHA-256 digest:' "$RUNNER_TEMP/fleet-159.signer")
          echo 'Signature matches stable signed 159 (same fleet signer as 153/157).' > "$RUNNER_TEMP/cdn-apk-inputs/fleet-signature-match.txt"
          sha256sum "$apk" > "$RUNNER_TEMP/cdn-apk-inputs/apk.sha256"
          echo "path=$apk" >> "$GITHUB_OUTPUT"
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: ${{ inputs.emulator_only != true }}
        with:
          name: maestrovpn-mobile-160-${{ github.sha }}
          path: |
            ${{ steps.apk.outputs.path }}
            ${{ runner.temp }}/cdn-apk-inputs/
          if-no-files-found: error
          retention-days: 14
      - name: Run focused JVM checks and package native emulator tests
        if: ${{ inputs.emulator_review == true }}
        env:
          LOCAL_PROPERTIES: ${{ secrets.LOCAL_PROPERTIES }}
        run: |
          set -euo pipefail
          chmod +x ./gradlew
          test_args=()
          if [ "${{ inputs.visual_preview }}" = true ]; then
            test_args=(
              :app:testOtherDebugUnitTest
              --tests 'com.maestrovpn.tv.compose.screen.tvhome.LivingEyeLayerGeometryTest'
              --tests 'com.maestrovpn.tv.whitelist.WhiteListRuntimeTest'
            )
          elif [ "${{ inputs.emulator_only }}" != true ]; then
            test_args=(
              :app:testOtherDebugUnitTest
              --tests 'com.maestrovpn.tv.compose.screen.tvhome.LivingEyeLayerGeometryTest'
            )
          fi
          ./gradlew "${test_args[@]}" :app:assembleOtherDebug :app:assembleOtherDebugAndroidTest \
            -PmaestroCdnApk=true -PmaestroEmulatorApk=true --stacktrace --no-daemon
          mapfile -t emulator_apks < <(find app/build/outputs/apk/other/debug -maxdepth 1 -type f -name '*-emulator*.apk')
          mapfile -t test_apks < <(find app/build/outputs/apk/androidTest/other/debug -maxdepth 1 -type f -name '*.apk')
          [ "${#emulator_apks[@]}" -eq 1 ]
          [ "${#test_apks[@]}" -eq 1 ]
          [ "$(apkanalyzer manifest application-id "${emulator_apks[0]}")" = 'com.maestrovpn.tv' ]
          [ "$(apkanalyzer manifest application-id "${test_apks[0]}")" = 'com.maestrovpn.tv.test' ]
          mkdir -p "$RUNNER_TEMP/mobile-emulator-apks"
          cp "${emulator_apks[0]}" "$RUNNER_TEMP/mobile-emulator-apks/app.apk"
          cp "${test_apks[0]}" "$RUNNER_TEMP/mobile-emulator-apks/android-test.apk"
          python3 - <<'PY'
          import os, pathlib, zipfile
          apk = pathlib.Path(os.environ['RUNNER_TEMP']) / 'mobile-emulator-apks/app.apk'
          with zipfile.ZipFile(apk) as z:
              names = z.namelist()
              assert 'lib/x86_64/libbox.so' in names, 'Native x86_64 libbox is required'
              abis = {n.split('/')[1] for n in names if n.startswith('lib/') and n.endswith('.so')}
              assert abis == {'x86_64'}, f'Unexpected emulator APK ABIs: {abis}'
          PY
          pushd "$RUNNER_TEMP/mobile-emulator-apks" >/dev/null
          sha256sum app.apk android-test.apk > apks.sha256
          printf '%s\n' "$GITHUB_SHA" > apk-source.txt
          popd >/dev/null
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: ${{ inputs.emulator_review == true }}
        with:
          name: maestrovpn-mobile-emulator-160-${{ github.sha }}
          path: ${{ runner.temp }}/mobile-emulator-apks/
          if-no-files-found: error
          retention-days: 14
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: ${{ always() && inputs.emulator_review == true && (inputs.emulator_only != true || inputs.visual_preview == true) }}
        with:
          name: maestrovpn-mobile-jvm-160-${{ github.sha }}
          path: |
            app/build/test-results/testOtherDebugUnitTest/
            app/build/reports/tests/testOtherDebugUnitTest/
          if-no-files-found: warn
          retention-days: 14
      - name: Remove runner signing file
        if: ${{ always() }}
        run: rm -f -- app/release.keystore
  phone-validation:
    needs: apk
    if: ${{ inputs.emulator_review == true }}
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: android-actions/setup-android@9fc6c4e9069bf8d3d10b2204b1fb8f6ef7065407
      - name: Download the native emulator and instrumentation APKs
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/mobile-emulator-apks"
          gh run download "$GITHUB_RUN_ID" --repo "$GITHUB_REPOSITORY" \
            -n "maestrovpn-mobile-emulator-160-$GITHUB_SHA" -D "$RUNNER_TEMP/mobile-emulator-apks"
          pushd "$RUNNER_TEMP/mobile-emulator-apks" >/dev/null
          [ "$(cat apk-source.txt)" = "$GITHUB_SHA" ]
          sha256sum -c apks.sha256
          popd >/dev/null
      - name: Run focused phone instrumentation on native Android 34
        timeout-minutes: 20
        env:
          ANDROID_SERIAL: emulator-5554
          INSTRUMENTATION_CLASSES: ${{ inputs.visual_preview && 'com.maestrovpn.tv.compose.PhoneEyePreviewInstrumentedTest#homeEyeMotionPreview' || inputs.emulator_only && 'com.maestrovpn.tv.compose.PhoneComponentGeometryInstrumentedTest#awaitingPaymentUsesOnlyFixtureCallbacks' || 'com.maestrovpn.tv.compose.PhoneNavigationInstrumentedTest,com.maestrovpn.tv.compose.PhoneComponentGeometryInstrumentedTest' }}
          MAESTRO_VISUAL_PREVIEW: ${{ inputs.visual_preview }}
        run: |
          set -euo pipefail
          output="$RUNNER_TEMP/mobile-validation"
          mkdir -p "$output"
          export MAESTRO_VALIDATION_OUT="$output"
          printf '%s\n' "$GITHUB_SHA" > "$output/apk-source.txt"
          collect_evidence() {
            status=$?
            trap - EXIT
            set +e
            timeout 15 adb logcat -b crash -d > "$output/crash-logcat.txt" 2>&1
            timeout 15 adb logcat -d -v threadtime > "$output/logcat.txt" 2>&1
            timeout 15 adb shell dumpsys window > "$output/window.txt" 2>&1
            timeout 15 adb exec-out screencap -p > "$output/final-screen.png" 2> "$output/final-screen-error.txt"
            timeout 30 adb pull /sdcard/Android/data/com.maestrovpn.tv/files/ui-captures "$output/" > "$output/captures-pull.txt" 2>&1
            if grep -qi 'Permission denied' "$output/captures-pull.txt"; then
              timeout 15 adb root > "$output/adb-root.txt" 2>&1
              timeout 15 adb wait-for-device
              timeout 30 adb pull /sdcard/Android/data/com.maestrovpn.tv/files/ui-captures "$output/" > "$output/captures-root-pull.txt" 2>&1
            fi
            timeout 10 adb emu kill >/dev/null 2>&1
            exit "$status"
          }
          trap collect_evidence EXIT
          export ANDROID_USER_HOME="$RUNNER_TEMP/maestro-android-34"
          export ANDROID_EMULATOR_HOME="$ANDROID_USER_HOME"
          export ANDROID_AVD_HOME="$ANDROID_USER_HOME/avd"
          mkdir -p "$ANDROID_AVD_HOME"
          sdkmanager 'emulator' 'system-images;android-34;google_apis;x86_64' >/dev/null
          echo 'no' | avdmanager create avd -n maestro-phone-native -k 'system-images;android-34;google_apis;x86_64' \
            --device 'pixel_4' --path "$ANDROID_AVD_HOME/maestro-phone-native.avd" --force
          printf 'avd.ini.encoding=UTF-8\npath=%s\ntarget=android-34\n' "$ANDROID_AVD_HOME/maestro-phone-native.avd" > "$ANDROID_AVD_HOME/maestro-phone-native.ini"
          python3 - <<'PY'
          import os, pathlib
          path = pathlib.Path(os.environ['ANDROID_AVD_HOME']) / 'maestro-phone-native.avd/config.ini'
          settings = {'hw.lcd.width': '1080', 'hw.lcd.height': '2340', 'hw.lcd.density': '450', 'hw.keyboard': 'no'}
          lines = [line for line in path.read_text().splitlines() if line.partition('=')[0].strip() not in settings]
          path.write_text('\n'.join(lines + [f'{key}={value}' for key, value in settings.items()]) + '\n')
          PY
          sudo chmod 666 /dev/kvm
          "$ANDROID_SDK_ROOT/emulator/emulator" -avd maestro-phone-native -port 5554 -no-window -no-audio \
            -no-snapshot -no-boot-anim -gpu swiftshader_indirect -memory 3072 > "$output/emulator.log" 2>&1 &
          emulator_pid=$!
          boot_deadline=$((SECONDS + 180))
          timeout 60 adb wait-for-device
          while [ "$(timeout 5 adb shell getprop sys.boot_completed | tr -d '\r')" != 1 ]; do
            kill -0 "$emulator_pid"
            [ "$SECONDS" -lt "$boot_deadline" ]
            sleep 2
          done
          [ "$(adb shell getprop ro.product.cpu.abi | tr -d '\r')" = x86_64 ]
          adb shell getprop ro.product.cpu.abilist > "$output/device-abis.txt"
          adb shell wm size 1080x2340
          adb shell wm density 450
          adb shell settings put global window_animation_scale 0
          adb shell settings put global transition_animation_scale 0
          adb shell settings put global animator_duration_scale 0
          if [ "$MAESTRO_VISUAL_PREVIEW" = true ]; then
            adb shell settings put global animator_duration_scale 1
          fi
          adb shell input keyevent 82
          adb logcat -c
          adb install --abi x86_64 -r "$RUNNER_TEMP/mobile-emulator-apks/app.apk"
          adb install --abi x86_64 -r -t "$RUNNER_TEMP/mobile-emulator-apks/android-test.apk"
          adb shell pm grant com.maestrovpn.tv android.permission.CAMERA
          adb shell pm grant com.maestrovpn.tv android.permission.POST_NOTIFICATIONS
          set +e
          timeout 600 adb shell am instrument -w -r -e class "$INSTRUMENTATION_CLASSES" \
            com.maestrovpn.tv.test/androidx.test.runner.AndroidJUnitRunner 2>&1 | tee "$output/instrumentation.txt"
          instrumentation_exit=${PIPESTATUS[0]}
          set -e
          printf '%s\n' "$instrumentation_exit" > "$output/instrumentation-exit.txt"
          [ "$instrumentation_exit" -eq 0 ]
          if [ "$MAESTRO_VISUAL_PREVIEW" = true ]; then
            adb shell wm size 1920x1080
            adb shell wm density 160
            adb shell settings put system accelerometer_rotation 0
            adb shell settings put system user_rotation 0
            set +e
            timeout 120 adb shell am instrument -w -r -e preview_form_factor tv \
              -e class com.maestrovpn.tv.compose.PhoneEyePreviewInstrumentedTest#tvHomeLayoutPreview \
              com.maestrovpn.tv.test/androidx.test.runner.AndroidJUnitRunner 2>&1 | tee "$output/tv-instrumentation.txt"
            tv_exit=${PIPESTATUS[0]}
            set -e
            printf '%s\n' "$tv_exit" > "$output/tv-instrumentation-exit.txt"
            [ "$tv_exit" -eq 0 ]
            if ! timeout 30 adb pull /sdcard/Android/data/com.maestrovpn.tv/files/ui-captures "$output/" > "$output/preview-captures-pull.txt" 2>&1; then
              grep -qi 'Permission denied' "$output/preview-captures-pull.txt"
              timeout 15 adb root > "$output/preview-adb-root.txt" 2>&1
              timeout 15 adb wait-for-device
              timeout 30 adb pull /sdcard/Android/data/com.maestrovpn.tv/files/ui-captures "$output/" > "$output/preview-captures-root-pull.txt" 2>&1
            fi
          fi
          python3 - <<'PY'
          import os, pathlib, re
          output = pathlib.Path(os.environ['MAESTRO_VALIDATION_OUT'])
          visual_preview = os.environ.get('MAESTRO_VISUAL_PREVIEW') == 'true'
          logs = ['instrumentation.txt'] + (['tv-instrumentation.txt'] if visual_preview else [])
          expected = {target.split('#', 1)[0] for target in os.environ['INSTRUMENTATION_CLASSES'].split(',')}
          for name in logs:
              text = (output / name).read_text(errors='replace')
              assert re.search(r'OK \([1-9][0-9]* tests?\)', text), f'{name}: no successful test run'
              assert re.search(r'^INSTRUMENTATION_CODE:\s*-1\s*$', text, re.M), f'{name}: did not finish successfully'
              codes = [int(value) for value in re.findall(r'^INSTRUMENTATION_STATUS_CODE:\s*(-?\d+)', text, re.M)]
              assert codes and all(code >= 0 for code in codes), f'{name}: failure, error, or skipped test'
              seen = {value.strip() for value in re.findall(r'^INSTRUMENTATION_STATUS: class=(.+)$', text, re.M)}
              assert expected <= seen, f'{name}: missing classes {sorted(expected - seen)}'
          if visual_preview:
              captures = output / 'ui-captures'
              for name in ['fixture-eye-home-off.png', 'fixture-eye-home-on.png',
                           'fixture-eye-home-off-after.png', 'fixture-tv-home.png', 'fixture-eye-home-motion.mp4']:
                  file = captures / name
                  assert file.is_file() and file.stat().st_size > 0, f'Missing native preview: {name}'
              # screenrecord exits before the fixture ends; require a completed MP4 container.
              data = (captures / 'fixture-eye-home-motion.mp4').read_bytes()
              atoms, offset = set(), 0
              while offset + 8 <= len(data):
                  size = int.from_bytes(data[offset:offset + 4], 'big')
                  kind = data[offset + 4:offset + 8]
                  header = 8
                  if size == 1:
                      size = int.from_bytes(data[offset + 8:offset + 16], 'big'); header = 16
                  elif size == 0:
                      size = len(data) - offset
                  assert size >= header and offset + size <= len(data), 'Incomplete native MP4 atom'
                  atoms.add(kind)
                  offset += size
              assert offset == len(data) and {b'ftyp', b'moov', b'mdat'} <= atoms, 'Native MP4 is not finalized'
          PY
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: ${{ always() }}
        with:
          name: maestrovpn-mobile-validation-160-${{ github.sha }}
          path: ${{ runner.temp }}/mobile-validation/
          if-no-files-found: warn
          retention-days: 14
  phone-screens:
    needs: apk
    if: ${{ always() && inputs.capture_only == true && inputs.emulator_review != true }}
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: android-actions/setup-android@9fc6c4e9069bf8d3d10b2204b1fb8f6ef7065407
      - name: Capture the existing native payment fixture while its surface is visible
        timeout-minutes: 18
        env:
          GH_TOKEN: ${{ github.token }}
          ANDROID_SERIAL: emulator-5554
          SOURCE_RUN: '34422480557'
          SOURCE_SHA: e0b737d864e86062e1e5c3fe2fdfb26e663130f8
          SOURCE_ARTIFACT: '10131631683'
          SOURCE_ZIP_SHA256: bed03e41c00a576462a9362bb0c1a62616903ab477ced76092595b12a65ae22e
        run: |
          set -euo pipefail
          export CAPTURE_OUT="$RUNNER_TEMP/mobile-screens"
          mkdir -p "$CAPTURE_OUT" "$RUNNER_TEMP/captured-native"
          collect_evidence() {
            status=$?
            trap - EXIT
            set +e
            timeout 15 adb logcat -b crash -d > "$CAPTURE_OUT/crash-logcat.txt" 2>&1
            timeout 15 adb logcat -d -v threadtime > "$CAPTURE_OUT/logcat.txt" 2>&1
            timeout 30 adb pull /sdcard/Android/data/com.maestrovpn.tv/files/ui-captures "$CAPTURE_OUT/" > "$CAPTURE_OUT/captures-pull.txt" 2>&1
            if grep -qi 'Permission denied' "$CAPTURE_OUT/captures-pull.txt"; then
              timeout 15 adb root > "$CAPTURE_OUT/adb-root.txt" 2>&1
              timeout 15 adb wait-for-device
              timeout 30 adb pull /sdcard/Android/data/com.maestrovpn.tv/files/ui-captures "$CAPTURE_OUT/" > "$CAPTURE_OUT/captures-root-pull.txt" 2>&1
            fi
            timeout 10 adb emu kill >/dev/null 2>&1
            exit "$status"
          }
          trap collect_evidence EXIT
          gh api "repos/$GITHUB_REPOSITORY/actions/artifacts/$SOURCE_ARTIFACT/zip" > "$RUNNER_TEMP/captured-native.zip"
          python3 - <<'PY'
          import hashlib, json, os, pathlib, shutil, zipfile
          root = pathlib.Path(os.environ['RUNNER_TEMP'])
          archive = root / 'captured-native.zip'
          assert hashlib.sha256(archive.read_bytes()).hexdigest() == os.environ['SOURCE_ZIP_SHA256']
          with zipfile.ZipFile(archive) as z:
              for name in ('app.apk', 'android-test.apk', 'apks.sha256', 'apk-source.txt'):
                  entry, = [n for n in z.namelist() if pathlib.PurePosixPath(n).name == name]
                  with z.open(entry) as source, (root / 'captured-native' / name).open('wb') as target:
                      shutil.copyfileobj(source, target)
          assert (root / 'captured-native/apk-source.txt').read_text().strip() == os.environ['SOURCE_SHA']
          with zipfile.ZipFile(root / 'captured-native/app.apk') as z:
              assert 'lib/x86_64/libbox.so' in z.namelist()
              assert {n.split('/')[1] for n in z.namelist() if n.startswith('lib/') and n.endswith('.so')} == {'x86_64'}
          receipt = {key: os.environ[key] for key in ('SOURCE_RUN', 'SOURCE_SHA', 'SOURCE_ARTIFACT', 'SOURCE_ZIP_SHA256')}
          (pathlib.Path(os.environ['CAPTURE_OUT']) / 'source.json').write_text(json.dumps(receipt, indent=2))
          PY
          pushd "$RUNNER_TEMP/captured-native" >/dev/null
          sha256sum -c apks.sha256
          popd >/dev/null
          export ANDROID_USER_HOME="$RUNNER_TEMP/maestro-payment-android-34"
          export ANDROID_EMULATOR_HOME="$ANDROID_USER_HOME"
          export ANDROID_AVD_HOME="$ANDROID_USER_HOME/avd"
          mkdir -p "$ANDROID_AVD_HOME"
          sdkmanager 'emulator' 'system-images;android-34;google_apis;x86_64' >/dev/null
          echo 'no' | avdmanager create avd -n maestro-payment -k 'system-images;android-34;google_apis;x86_64' \
            --device 'pixel_4' --path "$ANDROID_AVD_HOME/maestro-payment.avd" --force
          printf 'avd.ini.encoding=UTF-8\npath=%s\ntarget=android-34\n' "$ANDROID_AVD_HOME/maestro-payment.avd" > "$ANDROID_AVD_HOME/maestro-payment.ini"
          python3 - <<'PY'
          import os, pathlib
          path = pathlib.Path(os.environ['ANDROID_AVD_HOME']) / 'maestro-payment.avd/config.ini'
          settings = {'hw.lcd.width': '1080', 'hw.lcd.height': '2340', 'hw.lcd.density': '450', 'hw.keyboard': 'no'}
          lines = [line for line in path.read_text().splitlines() if line.partition('=')[0].strip() not in settings]
          path.write_text('\n'.join(lines + [f'{key}={value}' for key, value in settings.items()]) + '\n')
          PY
          sudo chmod 666 /dev/kvm
          "$ANDROID_SDK_ROOT/emulator/emulator" -avd maestro-payment -port 5554 -no-window -no-audio \
            -no-snapshot -no-boot-anim -gpu swiftshader_indirect -memory 3072 > "$CAPTURE_OUT/emulator.log" 2>&1 &
          emulator_pid=$!
          boot_deadline=$((SECONDS + 180))
          timeout 60 adb wait-for-device
          while [ "$(timeout 5 adb shell getprop sys.boot_completed | tr -d '\r')" != 1 ]; do
            kill -0 "$emulator_pid"
            [ "$SECONDS" -lt "$boot_deadline" ]
            sleep 2
          done
          [ "$(adb shell getprop ro.product.cpu.abi | tr -d '\r')" = x86_64 ]
          adb shell wm size 1080x2340
          adb shell wm density 450
          adb shell settings put global window_animation_scale 0
          adb shell settings put global transition_animation_scale 0
          adb shell settings put global animator_duration_scale 0
          adb shell input keyevent 82
          adb install --abi x86_64 -r "$RUNNER_TEMP/captured-native/app.apk"
          adb install --abi x86_64 -r -t "$RUNNER_TEMP/captured-native/android-test.apk"
          adb shell pm grant com.maestrovpn.tv android.permission.CAMERA
          adb shell pm grant com.maestrovpn.tv android.permission.POST_NOTIFICATIONS
          adb logcat -c
          python3 - <<'PY'
          import json, os, pathlib, re, subprocess, threading, time
          output = pathlib.Path(os.environ['CAPTURE_OUT'])
          frames = output / 'frames'; frames.mkdir()
          method = 'awaitingPaymentUsesOnlyFixtureCallbacks'
          target = 'com.maestrovpn.tv.compose.PhoneComponentGeometryInstrumentedTest#' + method
          started, finished = threading.Event(), threading.Event()
          origin = time.monotonic()
          def capture_frames():
              if not started.wait(90):
                  return
              next_frame = time.monotonic()
              with (output / 'frames.jsonl').open('w') as log:
                  for index in range(30):
                      if finished.is_set(): break
                      path = frames / f'frame-{index:02d}.png'
                      at = time.monotonic() - origin
                      try:
                          with path.open('wb') as file:
                              result = subprocess.run(['adb', 'exec-out', 'screencap', '-p'], stdout=file, stderr=subprocess.PIPE, timeout=5)
                          log.write(json.dumps(dict(frame=path.name, elapsed_seconds=round(at, 3), size=path.stat().st_size, exit=result.returncode, error=result.stderr.decode(errors='replace'))) + '\n')
                          log.flush()
                          if result.returncode: break
                      except subprocess.TimeoutExpired:
                          log.write(json.dumps(dict(frame=path.name, elapsed_seconds=round(at, 3), error='screencap_timeout')) + '\n'); log.flush()
                          break
                      next_frame += 0.2
                      finished.wait(max(0, next_frame - time.monotonic()))
          sampler = threading.Thread(target=capture_frames, daemon=True)
          sampler.start()
          command = ['timeout', '180', 'adb', 'shell', 'am', 'instrument', '-w', '-r', '-e', 'class', target,
                     'com.maestrovpn.tv.test/androidx.test.runner.AndroidJUnitRunner']
          try:
              with subprocess.Popen(command, stdout=subprocess.PIPE, stderr=subprocess.STDOUT, text=True, bufsize=1) as process, (output / 'instrumentation.txt').open('w') as log:
                  for line in process.stdout:
                      log.write(line); log.flush(); print(line, end='', flush=True)
                      if line.strip() == 'INSTRUMENTATION_STATUS: test=' + method:
                          started.set()
                  exit_code = process.wait()
              (output / 'instrumentation-exit.txt').write_text(str(exit_code))
          finally:
              finished.set(); sampler.join(timeout=6)
          text = (output / 'instrumentation.txt').read_text()
          assert exit_code == 0 and re.search(r'OK \(1 tests?\)', text), 'The single payment fixture did not finish successfully'
          assert re.search(r'^INSTRUMENTATION_CODE:\s*-1\s*$', text, re.M)
          assert all(int(code) >= 0 for code in re.findall(r'^INSTRUMENTATION_STATUS_CODE:\s*(-?\d+)', text, re.M))
          assert started.is_set() and any(path.stat().st_size for path in frames.glob('*.png')), 'No native frames were captured during the payment fixture'
          PY
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: ${{ always() }}
        with:
          name: maestrovpn-mobile-screens-160-${{ github.sha }}
          path: ${{ runner.temp }}/mobile-screens/
          if-no-files-found: warn
          retention-days: 14
