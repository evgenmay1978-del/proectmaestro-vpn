# Здоровье серверов — 10.09.2026, 23:41–23:45 МСК

Read-only проверка по запросу владельца. Фактические численные данные, состояние служб и размеры каталогов: SERVER_HEALTH_2026-09-10.json. Службы не перезапускались, файлы на серверах не удалялись, настройки не менялись. Это инфраструктурное наблюдение, не проверка работоспособности всех VPN-сессий.

| Сервер | Наблюдаемый результат | Что требует внимания |
|---|---|---|
| S1 | Собственный диагностический HTTP-сервис вернул200, ожидаемое тело, 0,093278с с S4. | Прямой SSH закрылся до авторизации; через S4 — banner timeout. OS/память/диск/службы не прочитаны. Причина ограничения SSH не установлена; сервер не считать упавшим. |
| S2 | Все найденные действующие VPN/CDN/бот/БД/веб-службы active, failed units нет. CPU9,7% в1s; RAM доступно1275/1968МиБ. | Диск98%, свободно258318336bytes (~246МиБ); inode занято34,5%. Реальный риск исчерпания места. |
| S3 | VPN/CDN/rqlite active, failed units нет. Диск20%, свободно~30,1ГиБ; RAM доступно887/1830МиБ. | 1vCPU: в10s idle31,9%, rqlited50,8% CPU; CPU pressure avg60~21,75%. Начальные96% в1s не выдавать за постоянную нагрузку. |
| S4 | Private/commercial/ingress/обычный VPN/rqlite active, failed units нет. Диск55%, свободно~6,36ГиБ; RAM доступно1073/1968МиБ. | За10s idle71,7%, rqlited16,4%; высокий короткий load спадает. Private config и ingress SHA совпадают с восстановленными исходными. |

На S2–S4 за30мин нет обнаруженных OOM/storage/crash ошибок; kernel err за24ч без этих категорий. На S3 четыре ошибки аутентификации SSH. Это отфильтрованные журналы; отсутствие строк не равно полной проверке приложения. Загруженные как not-found mysql/mysqld/nginx наS3 не являются установленными упавшими службами.

## Место S2

Всего /var/tmp занимает1030860800bytes (~983МиБ):
- /var/tmp/maestro-mobile-candidate-1016002 —422309888bytes (~403МиБ).
- /var/tmp/maestro-ota-159 —399908864bytes (~381МиБ).
- /var/tmp/maestro-ota-160 —208461824bytes (~199МиБ).

Это кандидаты на разбор, а не разрешение удаления. 1016002 и подготовленные артефакты по текущему указанию сохранять; действующую OTA160/1016001 и rollback не затрагивать. До любой последующей разрешённой очистки проверить содержимое, отсутствие ссылок на активную раздачу и наличие сохранённой копии. Архивы/журналы/БД автоматически не чистить. Остальные размеры: /var/log~483МиБ (system journal96МиБ), /var/backups~499МиБ, /var/cache~169МиБ.

## Точка продолжения

Приоритет по здоровью — безопасно освободить место наS2 в рамках отдельного разрешения на очистку и установить причину ограничения SSH кS1 без изменения его работающих служб. S3 rqlite нагружает единственное ядро; этот снимок не устанавливает причину нагрузки и не разрешает менять БД, лимиты или коммерческую логику. Текущая private-CDN задача сохраняется: fixed-edge новый запрос после простоя работает, открытый ответ черезCDN усечён примерно60с; все опыты в PRIVATE_CDN_LIVE_RESULTS_2026-09-10.md.

