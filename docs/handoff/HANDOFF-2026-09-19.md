# MaestroVPN — HANDOFF на 19.09.2026, 23:10 UTC

> Сессия 18–19.09: восстановление после аварии rqlite, раздельные подписки, 4 CDN-ноды через Яндекс-эдж,
> перенос Германии на 8443, метринг со всех origin'ов, фикс /sub/ для новых клиентов.
> Полная память (разделы 1–44): `docs/handoff/CONTEXT-NEW-CHAT-2026-09-18-evening.md` (копия с устройства
> `/sdcard/Download/maestro-ops/`).

## 1. Что работает (проверено в этой сессии)

### Панели
- Контроллер коммерции: юнит `maestro-cdn-controller`, билд **87fe24eb**
  (`/var/backups/maestro-commercial-controller-20260904-s4-qzBchh/panel-candidate-87fe24eb/maestro-panel`),
  слушает `127.0.0.1:18910`. Умеет переживать недоступность rqlite (ретрай сборки рантайма вместо падения).
- Легаси-панель `maestro-panel` на `127.0.0.1:8910` — именно она рендерит **обычную** подписку для старых токенов
  (её env: `/etc/maestro-panel.env`, данные клиентов: `/var/lib/maestro/customers.json`).
- nginx публичный фронт: `https://wapmixx.ru:8911`.

### Подписки
- Обычная `/sub/<token>`: **4 VLESS (🇪🇸🇨🇿🇳🇱🇩🇪) + Naive (naive+https) + AnyTLS** — проверено публично.
- CDN `/cdn-sub/<token>`: гейт «активная обычная подписка + баланс ГБ > 0», **4 CDN-ноды** (по одной на сервер),
  у каждой свой путь `<DE-путь>/<cc>` и свой client-encryption.
- Новые клиенты (token вида `subscription_<hex>`) до фикса получали 404 на `/sub/` — **исправлено** (см. §3).

### Германия VLESS (перенос на 8443)
- `89.125.19.95:443` **блокируется российским DPI** (TCP-таймаут с телефона), `8443` — TLS-хендшейк 160 мс.
- Сделано: второй x-ui inbound id=2 на 8443 на S4 (те же REALITY-ключи/SNI, клиенты через `client_inbounds`),
  `S4_VLESS_INBOUND=2` + `S4_VLESS_PORT=8443` в `/etc/maestro-panel.env`, `vless4.Port=8443` во всех 43 записях
  `/var/lib/maestro/customers.json`, `S4_VLESS_PORT=8443` в runtime.env контроллера, UFW: 8443/tcp разрешён.
- Проверка: реальный xray-клиент по DE → HTTP 204; владелец подтвердил «vless стабилен на всех 4».

### Метринг и деньги
- Метры `maestro-flat-meter` (sampler + `/usage` на 18085) стоят на **S1, S2, S3, S4** (на S2/S3 до этого их не было —
  трафик 🇨🇿/🇳🇱 не списывался вообще).
- `/usr/local/sbin/maestro-flat-charge` (timer 2 мин) тянет **все 4 origin'а**; есть фильтр `UUID_RE`, отсекающий
  синтетического `flat-owner` (раньше давал 400 каждые 2 минуты).
- Проверено: покупка пакета (заказ → `paid-claim` → `POST /admin/order/<id>/confirm`) = **+5 000 000 000 Б ровно**;
  списание `POST /admin/flat-cdn/charge {"uuid":…,"bytes":N}` = **−N ровно**, повтор идемпотентен.
- Автосписание с реального трафика приложения идёт (десятки проводок `panel whitelist debit login=wapmix` в логе).

### CDN-цепочка (работает)
- Клиент → Яндекс-эдж (`cdn-test.wapmixx.ru` → 188.72.103.4) → nginx ingress на S4 (`/etc/maestro-cdn-ingress/nginx.conf`,
  0.0.0.0:28080) → origin: S1 `193.17.183.48:28080`, S2 `85.137.166.237:28080`, S3 `46.30.42.151:18081`,
  S4 `127.0.0.1:18097` (tee → canary 18081).
- Эдж пропускает **только путь с префиксом DE-хэша** `/static/main/video/segment.ts/2c988fd8…`,
  поэтому все страны ходят как `<DE-путь>/es|cz|nl|de` + `rewrite` в канонический путь страны.
- На все ответы добавлен `Cache-Control: no-store` (иначе эдж кешировал XHTTP-ответы и страны «отваливались»).
- Логи ingress пишутся в `/run/maestro-cdn-ingress/access.log` (**User=www-data**; в `/var/log` писать нельзя —
  попытка уронила сервис на ~2 минуты, это и было «ноды отвалились по очереди»).
- Пинг каждые 3 с с телефона (10 минут, 716 запросов): **0 отказов**; p50: es 153 / cz 116 / nl 109 / de 95 мс,
  TLS до эджа p50 92 мс. В логе ingress: 134 ответа 200, ноль 5xx, виден реальный трафик приложения.

## 2. Открыто (первым делом)

1. **🇨🇿 CDN у владельца периодически n/a.** Автотесты и логи origin показывают, что сессии обслуживаются
   (S2 origin: 171×200, ошибок padding 0). Версии:
   (a) остаточный кеш эджа по **старым** ключам — лечится сменой суффикса пути (`/cz` → `/cz2`) в
   `/var/lib/maestro/flat-cdn-node.json` (+ `flat-cdn-nodes.json`) и в ingress, затем обновить подписку;
   (b) таймаут приложения из-за двух промежуточных хопов (эдж → S4 → S2);
   (c) поведение padding у конкретного клиента.
   Данные для разбора: `/run/maestro-cdn-ingress/access.log` (там IP клиента и все пути),
   `journalctl -u maestro-flat-cdn` на S2, `/var/log/nginx/flat-cdn-origin.log` на S2.
2. **Тестовый клиент** для отладки: login `e2efin225730`, token `subscription_a586…`, UUID `bc056c4b-…`,
   ≈4,997 ГБ, срок до 2026-10-18. После разбора — удалить (soft-delete в rqlite: `status='deleted'` + удалить токены).
3. **Профилактика**: guard для x-ui конфигов — сравнивать набор client id в `/etc/x-ui/x-ui.db` и
   `/usr/local/x-ui/bin/config.json`, при расхождении `systemctl restart x-ui` (иначе новые клиенты не работают
   на узле — так сломалась Германия).
4. Легаси-идентификаторы товаров в `handlers/maestro_customer.py` (`wl-gb-5-v1`, `wl-gb-20-v1`, …) —
   актуальные только в `handlers/maestro_customer_cdn.py` (`wl-gb-N-20260906`). CDN-покупка идёт через второй модуль.

## 3. Что менялось в коде (ветка `codex/cdn-lease-decouple-20260918`)

- `backend/internal/api/legacy_subscription_overlay.go` — фолбэк в нативный обработчик подписки, когда легаси-upstream
  отвечает 404 (нужно для клиентов, созданных контроллером: их нет в легаси-сторе). **Важно:** условие только по статусу —
  `if recorder.status == http.StatusNotFound` (проверка `!streamed` не работает: тело 404 уже записано).
- `backend/cmd/maestro-panel/main.go` — сборка rqlite-рантайма в цикле с backoff 3→20 с вместо `log.Fatalf`.
- `backend/internal/api/controlplane_flat_cdn.go` — `/cdn-sub/` (гейт, 4 ноды, слияние xray-документа, лейблы стран).
- `backend/internal/controlplane/whitelist_admin_credit.go` — `DebitWhiteListUsage` (списание из счётчиков).
- `backend/internal/api/controlplane_panel_whitelist.go`, `controlplane_commercial.go`, `controlplane_port.go` — ручки
  `/admin/flat-cdn/charge`, `/admin/flat-cdn/clients`, `cdn_sub_url` в балансе.

## 4. Доступы (значения — на устройстве, в git не коммитим)

| Хост | IP | Доступ |
|---|---|---|
| S1 (панель, бот @MaestroSecureVPN_bot) | 193.17.183.48 | пароль в `/sdcard/Download/authorized_keys` |
| S2 (rqlite leader, бот @MaestroSecureNaive_bot) | 85.137.166.237 | пароль в `/etc/maestro-panel.env` на S1 |
| S3 | 46.30.42.151 | ключ на S1: `/root/.ssh/maestro_olcrtc_s3` |
| S4 (CDN ingress + canary, DE-нода) | 89.125.19.95 | ключ `~/work/sshjs/s1_key` |

Хелперы на устройстве (`~/work`): `s1cmd.cjs`, `s2cmd.cjs` (пароль в `~/work/s2pass.txt`), `s4cmd.cjs`,
`upload.cjs` (кладёт файлы **только на S1**), `ghbuild.cjs`/`ghdlp.cjs` (сборка/артефакт), `pings.cjs`+`an2.cjs`
(пинг CDN каждые 3 с), `soak.cjs`, `deploy_*.sh` (шаблоны деплоя панели).

## 5. Быстрые проверки

```bash
# панель жива
curl -s http://127.0.0.1:18910/healthz
# обычная подписка (4 VLESS + Naive + AnyTLS)
curl -s "http://127.0.0.1:18910/sub/<token>?format=links" | base64 -d | sed 's/.*#//'
# CDN-подписка (4 ноды, гейт по ГБ)
curl -s -o /dev/null -w '%{http_code}\n' "http://127.0.0.1:18910/cdn-sub/<token>"
# метры origin'ов
for h in 127.0.0.1 85.137.166.237 46.30.42.151 89.125.19.95; do curl -s -m5 http://$h:18085/usage; echo; done
# списание вручную
curl -s -X POST -H "Authorization: Bearer $ADMIN" -H 'Idempotency-Key: k1' -H 'content-type: application/json' \
     -d '{"uuid":"<uuid>","bytes":1000000}' http://127.0.0.1:18910/admin/flat-cdn/charge
```

## 6. Финальные цифры пинг-прогона (400 кругов × 4 страны, каждые 3 с)

- TLS до эджа: **400/400**, p50 92 мс, p90 134 мс.
- 🇪🇸 **400/400** (p50 152 мс), 🇨🇿 **400/400** (p50 116 мс), 🇳🇱 **400/400** (p50 108 мс), 🇩🇪 **399/400** (p50 97 мс; один 502 в 23:11:32Z).
- Итого 1600 запросов: 1599 успешных. Жалобы на n/a по 🇨🇿 автотестом **не воспроизводятся** → разбирать на стороне
  приложения (кеш подписки/таймаут) и сопоставлять по времени с `/run/maestro-cdn-ingress/access.log`.
