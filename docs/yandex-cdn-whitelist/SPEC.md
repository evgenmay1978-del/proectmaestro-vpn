# Specification

White-List entitlement is OFF by default. ACTIVE is additive: it publishes CDN nodes while ordinary subscription data stays unchanged. DISABLED/SUSPENDED remove only CDN nodes. The target data plane is multi-node on S1/S2/S3/S4; no production deployment is authorized in this bootstrap.
