# Deployment

Node agents apply validated immutable releases. Each S1/S2/S3/S4 sidecar uses a separate process/config/log/API/stats and an atomic current pointer. Existing 3x-ui is never the deployment target for this phase.
