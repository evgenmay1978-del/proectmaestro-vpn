# Test plan

Run documentation/redaction tests first, then sidecar config/release/rollback, direct/CDN GET-body/literal-edge/idle checks, OFF subscription regression, entitlement/metering/idempotency/ledger tests, client matrix and baseline comparisons. Live/CDN operations remain pending approval.
