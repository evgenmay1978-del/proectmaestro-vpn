# Transport presets

`MAESTRO_ADVANCED`, `GENERIC_XHTTP_GET_COMPAT` and `EXPERIMENTAL_RESUMABLE` are versioned capability contracts. Each pins binary provenance and records protection/client support. Experimental is never auto-published.
